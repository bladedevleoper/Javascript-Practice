//isNaN = tests that its not a number

function getRandomNumber(lower, upper){
if(isNaN('lower') || isNaN('upper')){

    //this throws an error to the console log
    throw new error('bpth arguements must be a number');
}

}